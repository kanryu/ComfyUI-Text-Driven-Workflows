[project]
name = "comfyui-text-driven-workflows"
description = "A smart ComfyUI extension that simplifies text-driven workflows. Systematically organize, aggregate, and switch prompts or character settings with a single click, and batch process multiple staging plans simultaneously. Now features interactive line-by-line prompt tuning with real-time weight sync, and a color-coded token counter to prevent SD 1.5/SDXL chunk degradation."
version = "1.0.0"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/kanryu/ComfyUI-Text-Driven-Workflows"

[tool.comfy]
PublisherId = "kanryu"
DisplayName = "ComfyUI Text-Driven Workflows"